spring:
  servlet:
    multipart:
      max-file-size: 200MB
      max-request-size: 200MB
  main:
    allow-bean-definition-overriding: true
  cloud:
    config:
      enabled: false

  datasource:
    url: 'jdbc:postgresql:************'

    username: postgres
    password:


ed:
  metrics:
    kafka-address: localhost:9092
    kafka-topic: test
    enabled: false

rest:  
  timeout:
    connect: 5
    read: 200
